# Core Flask dependencies
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Mail==0.9.1
python-dotenv==1.0.0
SQLAlchemy==2.0.23
Werkzeug==3.0.1

# Password hashing
bcrypt==4.1.2

# PDF generation
reportlab==4.0.7

# Email validation
email-validator==2.1.0

# Production server (optional for development)
# gunicorn==21.2.0  # Not needed on Windows for development

# PostgreSQL support (optional, only if using PostgreSQL)
# psycopg2-binary==2.9.9

# Image support for PDF (optional - install separately if needed)
# Pillow==10.1.0

# Data processing (optional - not required for core functionality)
# pandas==2.1.4
